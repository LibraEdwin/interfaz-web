// @ts-check
import { THEME_LIGHT } from '@styles';

/**
 * Estado por defecto de la app
 * @constant
 * @type {import('./appContext').AppContextInterface}
 */
const defaultState = {
  theme: {
    themeMode: THEME_LIGHT
  }
};

export default defaultState;
